<div class="w-full">
  <% content_for :title, "Accounts" %>

  <div class="flex flex justify-between items-center border-b border-gray-300 shadow-md shadow-slate-200 mb-2 pb-2">
    <%= link_to "Back to budgets", budgets_path, class: "btn btn-secondary" %>
    <span class="font-bold text-2xl dark:text-white">Accounts</span>
    <%= link_to "New account", new_budget_account_path(@budget), class: "btn btn-primary" %>
  </div>

  <div id="accounts" class="min-w-full">
    <%= render "list" %>
  </div>
</div>
