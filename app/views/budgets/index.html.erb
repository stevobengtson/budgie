<div class="w-full">
  <% content_for :title, "Budgets" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl dark:text-white">Budgets</h1>
    <%= link_to "New budget", new_budget_path, class: "rounded-lg py-3 px-5 bg-blue-500 hover:bg-blue-700 text-white block font-medium" %>
  </div>

  <div id="budgets" class="min-w-full">
    <%= render "list" %>
  </div>
</div>
