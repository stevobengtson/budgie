<th class="border-b dark:border-slate-600 font-bold p-4 pl-8 pt-0 pb-3 text-slate-500 dark:text-white-900 text-left">
    <% if @sortable %>
    <%= sortable @column, @title %>
    <% else %>
    <%= @title %>
    <% end %>
</th>